services:
  - type: web
    name: medibot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run medibot.py --server.port=$PORT --server.address=0.0.0.0
    envVars:
      - key: GROQ_API_KEY
        sync: false